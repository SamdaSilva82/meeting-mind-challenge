# Optional overrides for the API workspace. If you omit a key here, it is read
# from the monorepo root `.env` two levels up (`starter/.env` when using Turbo).

# GOOGLE_API_KEY=
# GEMINI_MODEL=gemini-2.5-flash
# API_PORT=3001
